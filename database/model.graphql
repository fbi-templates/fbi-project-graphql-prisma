enum GenderEnum {
  MALE
  FEMALE
}

type User {
  id: ID! @unique
  name: String! @unique
  email: String
  gender: GenderEnum!
  posts: [Post!]!
}

type Post {
  id: ID! @unique
  title: String @unique
  author: User!
}
